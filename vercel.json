{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "env": {
    "DATABASE_URL": "./prisma/dev.db"
  },
  "build": {
    "env": {
      "DATABASE_URL": "./prisma/dev.db"
    },
    "command": "prisma generate && next build"
  }
}
